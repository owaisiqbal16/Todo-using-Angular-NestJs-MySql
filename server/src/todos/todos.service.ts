import { Injectable } from '@nestjs/common';
import { Todo } from './interface/todo.iterface';

@Injectable()
export class TodosService {
    
    asunc findAll() : Promise<Item> {
        return await this.
    }
}
